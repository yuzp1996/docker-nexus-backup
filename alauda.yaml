steps:
  - name: Docker
    parallel:
      - name:  nexus-backup
        container: tools
        groovy:
          - |+
            def image = deploy.dockerBuildWithRegister(
              dockerfile: "Dockerfile",
              context: ".",
              credentials: "alaudak8s",
              address: "alaudak8s/nexus-backup",
              tag: "2.6"
            )
            image.start().push()